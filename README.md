# Сервис назначения ревьюеров для Pull Request'ов

## Обзор проекта

Сервис автоматического назначения ревьюеров для Pull Request'ов, который позволяет выполнять переназначение ревьюверов и получать список PR’ов, назначенных конкретному пользователю, а также управлять командами и активностью пользователей.

## Реализованный функционал

- **Управление Pull Request'ами:**
  - Автоматическое назначение до двух активных ревьюверов из команды автора при создании PR
  - Переназначение ревьюверов на случайного активного участника из той же команды
  - Получение списка всех PR'ов, назначенных конкретному пользователю
  - Идемпотентная операция Merge PR с защитой от изменений после слияния
- **Управление командами и пользователями:**
  - Создание и получение команд
  - Управление флагом активности пользователей
  - Массовая деактивация пользователей команды
  - Автоматическая блокировка назначений неактивных пользователей на ревью
- **Аналитика:**
  - Получение статистики назначений по пользователям и по PR
- **Валидация данных:**
  - Проверка правильности входных данных.
- **Логирование:**
  - Настроено логирование событий сервиса.
- **Тестирование:**
  - Реализованы E2E-тесты и нагрузочный тест.

## Стек технологий

Go, PostgreSQL, Docker, Swagger, pgx, ogen, chi, Vegeta.

## Архитектурные особенности

Проект использует **API-first подход**: HTTP-сервер, клиент и модели данных генерируются автоматически из OpenAPI спецификации с помощью [ogen](https://github.com/ogen-go/ogen).

Преимущества:

- Типобезопасность на уровне компиляции
- Автоматическая валидация запросов/ответов
- Синхронизация документации и кода

## Запуск проекта

Используйте Makefile:

```bash
make up-build
```

Или напрямую Docker Compose:

```bash
docker compose up -d --build
```

- API: `http://localhost:8080`.
- Документация: `http://localhost:8080/docs`.

## Остановка

```bash
make down
```

Или

```bash
docker compose down
```

## E2E-тестирование

```bash
make test
```

Используется `docker-compose.test.yaml` для тестового окружения.

## Нагрузочное тестирование

```bash
make loadtest
```

Результаты:

```bash
Running SLI validation test: duration=10s, rate=50 req/s

=== Test Results ===
Requests: 500
Success Rate: 100.0000%
Mean Latency: 1.320011ms
P95 Latency: 2.89375ms
P99 Latency: 3.33925ms

--- Status Codes ---
200: 393
201: 107

=== SLI Validation ===
Success Rate: 100.0000% (target: >= 99.900%) [PASS]
P95 Latency: 2.89375ms (target: <= 300ms) [PASS]

✓ All SLI targets met successfully
```

## Структура кода

```
├── cmd/
│   ├── loadtest/      # Код для нагрузочного тестирования
│   └── pr-assignment/ # Основное приложение сервиса
├── deployments/       # Файлы для деплоя (SQL скрипт)
├── internal/
│   ├── api/           # Сгенерированный код OpenAPI
│   ├── config/        # Конфигурация приложения
│   ├── errs/          # Доменные ошибки
│   ├── handler/       # Обработчик HTTP запросов
│   ├── logattr/       # Атрибуты логирования
│   ├── logger/        # Настройка логирования
│   ├── middlewares/   # HTTP middlewares (логирование)
│   ├── repos/         # Репозитории (работа с базой данных)
│   ├── server/        # HTTP сервер
│   ├── services/      # Сервисы (бизнес-логика)
│   └── storage/       # Абстракция для работы с хранилищем данных (PostgreSQL)
├── openapi/           # Спецификация OpenAPI и конфигурация кодогенерации
└── tests/             # E2E-тесты
```

## Принятые проектные решения

- Флаг активности влияет только на выбор ревьюверов, а автор PR может быть как активным, так и неактивным.​
- При деактивации пользователя, который назначен ревьювером в открытых PR, пытаемся переназначить его на другого активного участника команды, а если кандидатов нет — просто убираем его из списка ревьюверов.
- При массовой деактивации пользователей команды, они удаляются из открытых PR.
- Выбран формат uuid для всех id.

Изменения в OpenAPI:

- format: uuid для всех полей с id.
- Добавлены эндпоинты /health, /stats/assignments и team/deactivate.
- Добавлены проверки minLength, maxLength и pattern для названий и юзернеймов.
- Добавлены коды ошибок 400 Bad Request и 500 Internal Server Error.
- Добавлены поля operationId, чтобы сгенерировались красивые имена методов.
- Код для ошибки "Team already exists" изменен на 409.
- Добавлен ответ 404 для /users/getReview.
- У /pullRequest/reassign в примере old_reviewer_id заменен на old_user_id
